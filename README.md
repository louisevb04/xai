# XAI Bias Analysis: Heart disease prediction
This project investigates how explainable AI (XAI) methods can be used to detect gender bias in a heart disease prediction model.

Two explanation methods are used:
- SHAP (feature attribution)
- Counterfactual explanations (DiCE)

Install dependencies:
pip install -r requirements.txt
